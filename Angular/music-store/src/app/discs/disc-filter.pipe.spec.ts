import { DiscFilterPipe } from './disc-filter.pipe';

describe('DiscFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
